# Screen Brightness Detection App

This Python application reads the system screen brightness in real time.

pip install screen-brightness-control

## Output
- Brightness level as a percentage (0–100)
- Updates every 2 seconds

## Usage
```bash
python brightness_detector.py


